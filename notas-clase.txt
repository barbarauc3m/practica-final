servidor web + cliente python modifica el fichero para que cada vez q enviemos codigo de operacion, se envie cad de caracteres con username, op, fecha, hora


tmb servidor (cliente de servidor rpc), donde queramos, que va imprimiendo

declarar variable entorno


OPERACIONES A HACER:

Gestión de usuarios:
    REGISTER: Registrar nuevo usuario

    UNREGISTER: Eliminar usuario

    CONNECT: Conectarse al sistema (indica IP/puerto)

    DISCONNECT: Desconectarse

Gestión de archivos:
    PUBLISH: Publicar nombre de archivo (no el contenido)

    DELETE: Eliminar archivo publicado

    LIST_USERS: Listar usuarios conectados

    LIST_CONTENT: Listar archivos de un usuario

    GET_FILE: Descargar archivo de otro cliente (transferencia directa P2P)



Me he quedado en list_users. Falla porque solo pasa como parámetro la operación, pero client_handler espera una operación y un usuario. Aún así hay que cambiarlo, porque tengo apuntado que hay que pasar op, username, fecha y hora. 